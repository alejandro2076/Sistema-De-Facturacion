{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Ejecutar main.py (integratedTerminal)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "console": "integratedTerminal"
        }
    ]
}
